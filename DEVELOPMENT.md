# DEVELOPMENT — 技术决策记录

日期:2026-08-01。规格与验收由主会话(Fable)完成,实现由两个 Opus 5 子 agent 按 SPEC.md 并行执行(A:core+tests,B:UI+PWA)。

## 决策记录

### D1 平台:PWA,而非微信小程序 / RN / Flutter
- **选择**:纯静态 PWA(HTML/CSS/JS),无后端。
- **理由**:零安装门槛、离线可用、数据纯本地(健康数据敏感,隐私最优);开发环境(WSL2)下可用 Node 直接验证核心逻辑;后续要上小程序/原生可复用 core/ 纯函数层,或用 Capacitor 打包。
- **代价**:网页通知受限(见 D8);iOS 上 PWA 能力弱于安卓。

### D2 零依赖、无构建
- **选择**:不用任何 npm 依赖、框架、打包器;测试用 node 内置 test runner。
- **理由**:项目规模小,框架收益低;零依赖 = 十年后 clone 下来还能跑;`node --test` 即完整可复跑测试,无 lockfile/供应链问题。
- **代价**:无 TypeScript 类型约束 → 用 SPEC.md 显式契约 + 单测覆盖补偿(两个 agent 并行开发时契约即接口)。

### D3 训练引擎 = 纯函数状态机,时间外部注入
- **选择**:core/ 不碰 DOM/`Date.now()`/localStorage;`tick(state, nowMs)` 不可变更新;阶段边界按"上一边界 + 阶段时长"累加。
- **理由**:可测性(测试传模拟时钟);浏览器后台 tab 会节流定时器,回前台后一次 tick 用 while 跨越多个阶段边界,状态依然精确、计时不漂移。

### D4 打卡(streak)规则
- 只有**完整做完**的训练(finished=true)算当天打卡;中途结束的记录保留次数统计但不算打卡。
- 连续天数锚点:今天有打卡从今天算,否则从昨天算(今天还没练不清零)——习惯类应用通行惯例,避免早上看到 streak 归零的挫败感。

### D5 日期存本地 `YYYY-MM-DD` 字符串
- 记录只存本地日期分量字符串,不存时间戳;日期算术用 `Date.UTC` 做。
- **理由**:打卡按"用户的当天"计,时间戳跨时区/夏令时换算是习惯类应用的经典 bug 源。

### D6 训练方案数值依据
- 新手 3s/3s×10×2 组(Mayo Clinic 起步方案:收缩 3s、每组 10-15 次、每日 2-3 组);进阶 10s 保持(NHS 慢速收缩目标);快速爆发 1s/1s(NHS fast squeezes);6-12 周见效、勿排尿时练习等文案同源。
- ⚠️ **本次会话的联网检索通道故障**(代理后端模型下线,WebSearch/WebFetch 均不可用),健康文案由主模型既有知识按上述来源共识撰写,**上线前建议人工比对 Mayo/NHS 原文复核一遍**。

### D7 执行委派方式(过程记录)
- SPEC.md 先行,锁死函数签名/状态字段/DOM id,两个 Opus agent 并行互不通信;集成正确性由契约 + 主会话验收(SPEC §9:全量单测、node --check、id 对照、预缓存对照、HTTP 200 巡检、人工复核状态机与 streak 边界)保障。
- 实际结果:两个 agent 均运行在 claude-opus-5;38/38 测试全绿;UI agent 自发做了跨模块集成冒烟并抓到 SPEC 的一处真实缺陷(restSec=0 时 contract→contract 漏提示音),验收采纳其修正;核心 agent 发现 Node 24 的 `--test` 目录参数坑(见 D9)。全部裁量已回写 SPEC §10 与本文件 D10。

### D9 Node 24 的 `node --test` 目录参数坑
- Node ≥ 24 把 `node --test` 的位置参数按文件/glob 处理,`node --test tests/` 报 `MODULE_NOT_FOUND` 且**伪装成 1 个测试失败**(极易误判为代码问题)。`npm test` 已定为裸 `node --test`;glob 形式 `node --test 'tests/*.test.mjs'` 亦可。

### D10 验收采纳的实现裁量(全录)
- 阶段推进判定 = phase/setIndex/repIndex 任一变化(非仅 phase);
- `validateConfig` 接受数值字符串('7'→7),NaN/空串/非数值回落 standard;取整用 `Math.round`;
- `totals` 的 totalReps/totalDurationSec 含 `finished:false` 的中途记录,finishedSessions/activeDays 仅计完成;`finished` 严格 `=== true`;
- `#countdown` 在 idle/done 显示 `—`;暂停时冻结圆动画于当前位置,恢复/回前台按剩余时长续播;
- 通知权限被拒 → 回滚提醒开关并 alert;prepare 阶段不发声;音频加 12ms 起音包络防爆音;
- sw.js 仅回写 `ok && type==='basic'` 响应,断网导航回退 index.html;
- custom 方案不暴露 prepareSec 输入(沿用默认 3);`remainingTotalSec` 返回小数秒。

### D11 发布与部署(2026-08-01)
- **独立公开仓库 `wowayou/tigang-companion`**:沿用 my-projects 下"每项目一仓"的既有惯例(根目录实测并非 git 仓库);公开是 GitHub Pages 免费版的要求,项目本身无秘密(纯前端、无密钥、数据全在用户本地)。
- **Pages 走 Actions 工作流**(Source = GitHub Actions)而非 branch 模式:① 测试全绿才部署(质量闸门);② 不经 Jekyll;③ 部署目标横向可扩——`test` 与 `deploy-*` 解耦,加平台 = 加并列 job,配方集中在 DEPLOY.md。
- **可移植性设计**:相对路径 + `start_url`/`scope` = "." → 根路径/子路径零改动通吃;发版升 `sw.js` CACHE_NAME 的约定写进 CLAUDE.md 与 DEPLOY.md。

### D12 维持阶段做成开关默认关,而非全面改三段式(2026-08-02)
- **选择**:engine.js 加一个可选的 `hold` 相位,`holdSec=0` 时完全退化成 v1 的两段式(收紧结束直接计数),只有用户主动开启才变成三段式。
- **理由**:老用户的训练节奏、总时长感知、打卡统计口径(completedReps 计数时机)一律不变——这是纯加法而非改造现有行为。三段式对新手更友好(收紧到位后先"保持"一下再放,肌肉记忆更清楚),但不该强加给已经习惯两段式节奏的用户。
- **实现要点**:`holdOf(config)` 把"缺失/非法当 0"做成硬保证,使得任何不带 `holdSec` 的旧 config(v1 存档、外部裸对象)都自然安全退化,不需要显式迁移逻辑。

### D13 成就系统用 bestStreak(历史最长)而非当前 streak(2026-08-02)
- **选择**:`achievements.js` 里所有连续类徽章(三日不辍/一周不断/双周坚持/月度不断)判定用 `stats.longestStreak`,不用 `computeStreak`。
- **理由**:徽章是"成就"语义——拿到就该是拿到了。如果用当前 streak 判定,某天没打卡时昨天刚解锁的徽章会当场消失,这种"努力被收回"的负反馈比不给徽章还伤积极性。`longestStreak` 单调不减,徽章墙只会越来越满,符合游戏化设计的基本预期。
- **代价**:`bestStreak` 与统计页顶部展示的当前 `streak` 是两个数字,文案上需要注意别混用(完成面板同时展示两者时已在设计上区分:`#done-streak-num` 显示当前连续天数,`#done-next` 的进度条基于 bestStreak 到下一枚徽章的距离)。

### D14 语音方案与 iOS 手势限制、提示音方向性设计(2026-08-02)
- **选择**:用浏览器原生 `speechSynthesis`(零依赖、免许可)播报阶段名;`primeVoice()` 在开始按钮点击处理器里先 speak 一句 `volume:0` 的空白话"开锁"。
- **理由**:iOS Safari 要求 `speechSynthesis.speak` 的首次调用必须发生在用户手势的同步调用栈里,否则该次及后续调用会被静默丢弃且没有报错——不预热的话训练开始后语音会"哑火"且难以定位原因。AudioContext 同理需要在同一个点击处理器里创建/resume。
- **提示音方向性**:即便关闭语音,不同阶段的提示音也做出方向区分——收紧上行(提起来)、维持两声平音(稳住)、放松下行(放下去)、休息低长音(歇着)——只开提示音也能靠耳朵分辨该干什么,不必总盯着屏幕。
- **默认关闭语音**(`DEFAULT_SETTINGS.voice = false`):语音只能走扬声器外放(`utterance.volume` 控制不了输出路由),而本应用的典型使用场景是办公室久坐、卫生间、床上,首次训练就外放"收紧"是实打实的尴尬风险。把"不看屏幕也知道该做什么"这个需求交给上面的方向性提示音兜底,语音留作用户主动开启的增强项——默认值的错误代价在这里是不对称的:默认开的翻车成本远高于默认关的少一个功能。

### D15 图标为什么手写 PNG 编码器(2026-08-02)
- **选择**:`tools/make-icons.mjs` 只用 Node 内置 `zlib`(deflate)+ `Buffer` 手写最小 PNG 编码器(CRC32/IHDR/IDAT/IEND chunk),自己做矢量光栅化和 4×4 超采样抗锯齿,不引入任何图像库。
- **理由**:项目零依赖是硬约束(见 D2),而 iOS 的 `apple-touch-icon` 不支持 SVG(见下),必须有 PNG;项目又没有构建步骤,不能指望部署时动态生成——只能把脚本和产物都当"源码"对待:脚本进 tools/,PNG 产物直接提交进仓库。
- **根因**:iOS Safari 只认位图格式的 `apple-touch-icon`,声明 SVG 会导致添加到主屏时图标退化成系统按标题生成的文字缩写(本项目实测退化成"提"字)。

### D16 界面单屏化(2026-08-02)
- **动机**:v1 的方案卡默认展开(5 个胶囊 + 摘要行)在常见手机视口下会把引导圆挤出首屏,用户一打开就得先滚动才能看到最重要的"开始训练"入口。
- **选择**:方案卡默认折叠成一行摘要,点击展开;布局改 `.app` flex 列 + `min-height: calc(100dvh - tabbar - safe-area)`,`.coach` 用 `flex:1` 吃掉剩余空间——高屏上引导圆能居中而不是整体堆顶部,矮屏则靠 `min-height` 兜底不整页滚动。实测 390×620(较矮的机型)与 390×760(常见机型)两种视口高度均单屏不滚动。

### D17 训练中的教练层:要领提示 + 呼吸节拍(2026-08-02)
- **动机**(来自实际使用反馈):「维持时间稍微一长,就不知道怎么用劲了,会憋着」。憋气是凯格尔最常见的错误,而且不只是"没效果"——憋气会抬高腹压,对痔疮/盆底反而是反作用,属于必须当场纠正的问题。
- **选择**:两条并行的提示,各带开关、默认开:① `#coach-cue` 按阶段显示短要领(维持段 3 条轮播:正常呼吸别憋气 / 腹部大腿臀部放松 / 只用盆底发力);② `#breath` 一条 4s 吸 + 4s 呼的节拍条。
- **理由**:这些要领知识页里本来就有(与 Mayo/NHS 同源),但没人会在维持到第 6 秒时切去知识页看——**信息必须出现在用户正盯着的那块屏幕上**,否则等于不存在。要领文案解决"怎么用劲",呼吸节拍解决"憋着"。
- **实现取舍**:轮播下标由「本阶段已过去多久」算出,不另起定时器——因此暂停时自动停住,也不会和主循环产生第二个时间源。呼吸相位用 `breathAnchorMs` 锚定,开始/恢复时重置(暂停期间不该"凭空呼吸了几轮")。
- **视觉**:呼吸条刻意做得比总进度条更细、无渐变、低饱和——它表达节奏而非进度,两条鲜亮的横条挨在一起会被误读。
- **配套**:维持开关旁加了循序渐进提示(从 3-5 秒起步,能全程正常呼吸再加长),不自动改任何值——自动调参在健康类应用里容易适得其反。

### D18 计数口径的显示修正(2026-08-02)
- **问题**:放松期间界面报「第 5/12 次」,让人以为下一次已经开始。
- **根因**:引擎在收紧(或维持)结束的那一刻既 `completedReps++` 也 `repIndex++`,语义是"索引始终指向当前/即将进行的那次"——本身没错,但直接拿去显示就有歧义。
- **选择**:只改显示不改引擎(引擎语义有测试覆盖、也被 `remainingAfterPhaseSec` 依赖)。放松时改报「已完成 N/M 次」(此时 `repIndex` 恰好等于刚做完那次的 1 基序号),prepare 另给一条文案。
- **顺带明确**:每组最后一次收缩后没有放松段,所以每组只有 `repsPerSet-1` 次放松——这正是总时长公式里 `sets*(reps-1)*relax` 的由来,以前没人写下来过。

### D19 阶段过渡为什么"生硬",以及怎么修(2026-08-02)
- **诊断**:被感知为生硬的不是缩放(缩放本来就是整段时长的 ease-in-out,且末速为 0,自然减速进入下一阶段),而是**颜色和相位环在边界上的硬切**。
- **颜色**:v1 给每个阶段各写一条 `linear-gradient`,而渐变之间无法插值,所以配色是瞬间跳变的。改成可过渡的 `background-color` + 一层固定不变的叠加渐变保留立体感;`transition-property` 拆成 `transform, background-color, color, box-shadow`,JS 只改第一项的时长(阶段秒数),配色固定 `.55s`。
- **相位环**:第一版用 `@property` 注册 `--ring`/`--ring-color`/`--ring-track` 来补间,**这一步是错的,已回退**(见 D21)。现在环的重置与配色一律瞬时。
- **代价/教训**:headless 截图在 `--virtual-time-budget` 下会把过渡定格在中途,一度让人误判成"环不消耗、暖色没生效"。用 `--dump-dom` 读计算值(`--ring: 50`、conic-gradient 50%、琥珀色全对)才确认是截图假象——**验证动效时,计算值比截图可信**。

### D21 回退:边界上要更少的东西在动(2026-08-02)
- **反馈**:D19 上线后用户反馈"还是比较突兀",并给了一段真机录屏。
- **诊断**(逐帧看录屏,10fps 抽帧):D19 的修法方向错了。原本只是硬切,加了过渡之后,阶段边界那一瞬间变成**同时四样东西在动**——环从空扫回满、环色从青插值到琥珀(sRGB 插值经过一段浑浊的橄榄色)、圆在变色、阶段名在淡入。运动本身比硬切更抢眼。而且阶段名淡入 .34s,恰好是最需要读它的时刻最看不清。
- **修正原则**:**阶段边界上只让一样东西在动**。保留圆底色的 `.5s` 过渡(各阶段同属青色系,插值干净);环的重置与配色改回瞬时(移除 `@property` 注册与相关 transition);阶段名/要领的动画缩短到 .16s/.22s 且不从 0 起、不做位移。
- **理由**:阶段切换本来就有提示音 + 文字同时到达,此刻的瞬时变化是被预期的,不构成突兀;真正突兀的是"预期之外的运动"。**过渡不是越多越平滑**——同时发生的多段动画会互相争夺注意力,反而放大边界感。
- **教训**:第一次听到"生硬"就直接加 transition 是条件反射,应该先逐帧看清到底什么在跳。这次是用户的录屏(而不是我的 headless 截图)提供了决定性证据——虚拟时间下的截图会把过渡定格在中途,反而掩盖了问题。

### D20 空格键控制(2026-08-02)
- 空格 = 开始 / 暂停 / 继续。焦点在 `input/textarea/select/button/a` 或可编辑元素上时不拦截——按钮上的空格是浏览器原生的"激活"语义,拦下来会触发两次;设置弹窗打开时也不拦。其余情况 `preventDefault` 掉默认翻页。

### D22 呼吸不与训练阶段同步(2026-08-02)
- **问题**(用户截图):收紧阶段的要领写着「呼气,慢慢向上提起」,而下方呼吸节拍条同时显示「吸气」——两条提示直接打架。
- **根因**:把两套互不相容的模型塞在了一起。「发力时呼气」来自盆底康复的一种教法(呼气可降低腹压),而节拍条是独立跑的固定周期。
- **判断:呼吸必须与阶段解耦**,理由是原理性的而非偏好——维持段可能长达数十秒,期间必然要正常呼吸好几轮,**呼吸相位在原理上就无法与训练阶段同步**。任何「呼气时收紧」的提示一旦遇到维持段就失效。而且知识页本来就写的是「收紧时正常呼吸、不要憋气」,是那条 contract 要领自己跑偏了,和自家知识页也矛盾。
- **处置**:`COACH_CUES` 里禁止出现指定呼吸相位的文案(已写进 SPEC §8.y 作为硬约束,并在代码里留了醒目注释);关于呼吸只说「别憋气 / 保持呼吸」,节奏交给节拍条。
- **教训**:两个功能各自看都对,拼在同一屏才暴露矛盾。新增引导类文案时要检查它是否隐含了某个与其他模块冲突的模型。

### D23 连贯感:唯一在动的那个过渡要慢(2026-08-02)
- **反馈**:D21 把边界改成"只剩圆底色在过渡"之后,用户反馈"缺少连贯感"——即从"太吵"走到了另一头"太干"。
- **修正**:圆底色过渡 `.5s → .9s`。当边界上只剩一样东西在动时,它承担了全部的"承上启下"职责,必须足够慢才连得起来;`.5s` 会让两个阶段显得各自独立。另外让相位环的**轨道色跨阶段不变**(只换弧色),留一个穿越所有边界都不变的视觉锚点。
- **心得**:D19(加了四样动效)→ D21(砍到只剩一样)→ D23(把这一样调慢),三轮才落到位。有用的判据不是"过渡多还是少",而是**同一时刻只允许一条视觉线索在变化,并且它要慢到足以跨越边界**。

### D24 撤销相位环与教练层(2026-08-02)
- **决定**:移除 `#coach-ring`(相位环)、`#coach-cue`(要领提示)、`#breath`(呼吸节拍)三组元素,以及 `settings.coachCue`/`settings.breath` 两个设置项。**D17、D19、D21、D22、D23 中与这三者相关的结论随之作废**,仅作过程记录保留。
- **理由**:用户实际使用后判定为噪音。回头看,训练页在很短时间里叠了四层「剩余时间/该干什么」的表达——圆的缩放、环的消耗、倒计时数字、要领文字,再加一条呼吸节拍——**同一个信息被讲了四遍**,注意力反而被分散。倒计时数字 + 圆的缩放已经足够。
- **代价**:「憋气」问题失去了训练页内的当场提示,只剩知识页里的静态说明。这是明确接受的取舍——用户自己是使用者,判断加提示的干扰大于收益。
- **教训**:D17 加教练层时的论证("信息必须出现在用户正盯着的那块屏幕上")本身没错,错在没有同时问「这块屏幕上已经有多少东西在争夺注意力」。**给一块已经很满的界面加信息,收益要按边际算,不能按绝对价值算。**
- 相位环从提出到删除历时数小时、跨 4 个版本(v2 引入 → v3 加补间 → v4 回退补间 → v5 调参 → v6 删除)。**当一个元素需要反复调参才能不碍事时,那通常是它本就不该存在的信号**,而不是参数还没调对。

### D25 放松属于「一次」,不是「两次之间的间隔」(2026-08-02)
- **反馈**:「为啥休息和结束的时候放松直接就被吞掉了」。
- **确认**:不是显示问题,是 v1 起就有的建模错误。旧状态机在收缩结束时判断 `repIndex < repsPerSet-1` 才进 `relax`,否则直接跳 `rest`/`done`。总时长公式里的 `sets*(reps-1)*relax` 就是这个模型的固化——**每组最后一次收缩没有放松**。
- **查证**(本次会话联网可用,与 D6 时不同):
  - Cleveland Clinic:「收紧 3 秒,然后放松 3 秒。**这就是一次凯格尔**」——放松被明确算作「一次」的组成部分。
  - Harvard Health:「每次保持 3-5 秒,**间隔相同秒数**休息」「每次重复之间要有意识地放松,放松时长与收缩相同」。
  - 三方口径一致:放松与收缩等长、且每次收缩都配一次放松。组间休息是在此之上的额外恢复,不能替代放松。
- **危害不止于观感**:收紧完最后一次,应用从未给过「松开」的指令。用户要么绷着进入休息,要么无引导地自行释放——每组的最后一次实际上变成了与其它次不同的动作。
- **修改**:收缩(或维持)结束一律进 `relax`;**索引推进移到放松结束**。总时长公式改为 `prepare + sets*reps*(contract+hold+relax) + (sets-1)*rest`。
- **副作用**:① 训练时长增加 `sets*relax`(新手 +6s、标准 +15s、进阶 +30s、快速 +2s);② 放松期间 `repIndex` 语义从「指向下一次」变成「指向刚做完的这次」,`#set-progress` 的已完成数改用 `repIndex+1`;③ 旧模型在 `restSec=0` 跨组时会出现 `contract→contract` 这种 phase 不变的边界,新模型下不再存在(app.js 里那处按索引变化判断阶段推进的防御逻辑因此成为冗余,但保留无害)。
- **教训**:这个错误在 v1 SPEC 里以「公式」的形式存在了很久,单看公式自洽、测试也全绿——因为测试是照着公式写的。**契约文档写错时,测试只会把错误固化下来**;真正发现它的是用户在真实节奏里感到「被吞掉了」。

### D26 全站计数:此刻多少人在做 + 总访问(2026-08-04)
- **需求**:顶栏加「当前有多少人在做」和「总访问人数」,参考站(zhuzhiliao)样式是一行浅色小字。
- **关键约束**:纯前端 + localStorage 统计不了「别人现在在做」——实时在线人数必须有服务器。项目零后端、零依赖、隐私优先,引不蒜子会把访客 IP / 页面 URL 发给第三方免费服务(无隐私政策、不稳定、数据可能清零),与项目基调冲突。
- **选择**:自建 **Cloudflare Worker + Durable Object**(免费计划)当那台「服务器」——WebSocket 会话 + 心跳算「在做/在线」,DO storage 累计「总访问」。数据只经 Cloudflare,不碰任何第三方统计。这是「零后端」约束的**唯一例外**,严格限定在 `worker/`(部署源,不进 sw.js 预缓存);core/ 依旧纯函数,客户端逻辑全在 app.js。
- **口径**:「在做」= `isRunning(session) && !session.paused`(暂停不算在做);每页加载记一次「总访问」(PV,与参考站「访问」同口径);本地预览(`file://`/localhost/私网)不 POST `/visit` 防污染,但仍连 WS 看实时数。
- **降级**:worker 未部署(`COUNTER_ORIGIN` 留空)或离线时,那行安静显示 `–`,不阻塞任何现有功能;WS 断线指数退避重连,后台断开、回前台即刷。
- **教训**:放顶栏中间会跟常驻的连续天数芯片抢位置、移动端宽度也不够;做成 header 第二行(整宽浅色小字)既不动现有元素、又能跟随吸顶随时看到——「此刻多少人在做」的陪伴感靠的是常驻可见。

### D27 数据导入口径 + 部署形态改「落地页 + /app/」(2026-08-04)
- **导入口径**:备份导入给两档——「合并」保留本机、只加指纹全新的记录(去重,重复导入同一文件不叠加);「替换」清空本机、完全按备份恢复(含设置)。核心逻辑放进 `core/storage.js` 的纯函数(`parseBackup` 净化校验 + `mergeRecords` 指纹去重),因为校验/合并是纯逻辑,必须可测;app.js 只做文件读取、对话框和落盘。
- **导出文件名**:旧实现固定 `tigang-data.json` 无区分度。改为 `tigang-YYYYMMDD-HHmmss.json`(与 time-logger 的 `timelog-*` 同款);iOS 主屏 PWA 的 `a.download` 经常只发请求不落盘,故 iOS 走 `navigator.share({files})` 让用户选「存储到文件」。
- **部署形态**:参考 time-logger 的 `time.eigentime.org/app/`——根路径放落地页(SEO + 分享入口),`/app/` 放应用。零构建地实现:新增 `site/`(落地页)+ `tools/build-site.mjs`(确定性复制:根=site/,/app/=sw.js 的 `PRECACHE_URLS` 单一真源),GitHub Pages 改传 `dist/`。`CNAME` 用「site/CNAME 存在即生效」的开关,默认不存在 → github.io 地址不中断。
- **代价**:老地址 `wowayou.github.io/tigang-companion/` 从「应用」变成「落地页」,应用移到 `/app/`;未启动过的项目可接受。sw.js 从此自预缓存(`./sw.js` 进清单),否则组装后 `/app/sw.js` 缺失、离线注册失败——time-logger 的清单也是这么写的。
- **英文名与域名**:产品面向中文用户,主品牌仍是「提肛陪伴」;英文名 **KegelMate**(落地页 tagline 用,可一行换)。域名定为 `kegel.eigentime.org`(用户拍板,与英文名呼应,而非 pinyin)。
- **重定向由 GitHub 自动完成**:自定义域名配置后,旧地址 `wowayou.github.io/tigang-companion/` 由 GitHub Pages 301 → `kegel.eigentime.org/`,项目不写重定向。这与 time-logger 不同(它用独立镜像仓库保留旧只读版)——我们同一 repo 服两个地址,无法留只读旧版;跨域名 localStorage 不通用,迁移靠导出/导入备份。落地页对 standalone 模式(老安装)直接跳进 `/app/`。

### D28 计数 worker 的 WebSocket Hibernation 坑(2026-08-05)
- **现象**:用户点「开始训练」后,顶栏「此刻多少人在做」仍显示 0。
- **根因**:DO 里 `ctx.acceptWebSocket(server)` 启用的是 **WebSocket Hibernation API**,消息必须由类方法 `webSocketMessage` / `webSocketClose` / `webSocketError` 接收;在 server 上 `addEventListener('message')` **不会触发**,所有客户端帧(hello / training / ping)被静默丢弃——「在做」标记永远不生效,且 `lastSeen` 不更新、会话还被 pruneStale 当作掉线剔除。线上实测证据:原始 WS 客户端握手 101、周期广播正常、但发 training 帧后无任何广播,40s 后收到 `close(1000,'stale')` → 帧根本没进 DO 的 JS。
- **修复**:改走 Hibernation API——`webSocketMessage(ws, message)` 处理 training(JSON.parse + 差量广播)与 ping(只刷 lastSeen),`webSocketClose(ws)` 移除会话并广播,`webSocketError` 兜底 close。会话注册仍用 `sessions` Map;DO 休眠唤醒后内存 Map 会清空,故 `webSocketMessage` 里「认不回会话就重建」。
- **连带注意**:Hibernation 下 `setInterval` 休眠期暂停,周期广播只是兜底;正确性靠「连接/变化即广播」+ 客户端回前台 `fetchStats()` 保证。`sessions` 是内存态、DO 重启即清零,对个人站量级可接受。
- **验证**:本地 `wrangler dev` 起 DO,原始 WS 客户端发 `training on:true` → 广播 `doing:1`,on→off→on 全链路正确;CI 重部署后线上 `/stats` 在训练会话存活期间返回 `doing:1`。
- **同类陷阱**:改 worker 里任何 WebSocket 逻辑前,先确认走的是 Hibernation 类方法还是 addEventListener——两者不可混用。

### D29 N1 训练完成分享卡(2026-08-05)
- **做法**:完成面板加「分享今天的成果」,`drawShareCard()` 在内存 canvas 画 1080×1440 成果卡(品牌 teal 渐变 + 同心圆环 + 今日收缩次数 + 连续天数 + 免责文案)→ PNG Blob → `navigator.canShare({files})` 支持就直接系统分享(Android Chrome),否则弹 `#dlg-share` 预览:iOS 长按保存 / 桌面「保存图片」下载。零新依赖,canvas 不占任何 DOM id。
- **关键取舍**:
  - 用「图片卡」而非「文案 + 链接」:图片在社交平台更易被看到、被转发(N1 服务分享裂变);文案只作 `navigator.share` 的附加 text。
  - 降级不用第三方分享 SDK(零依赖红线);iOS Safari 不支持分享文件,故预览弹窗兜底「长按保存」。
  - 卡片文案诚实:底部「免费 · 无广告 · 数据只在本地」,不制造虚假社会证明(ROADMAP 原则)。
- **验证**:headless Chromium 直接跑 `drawShareCard` 真实源码(从 app.js 按大括号配对抽取,避免手抄漂移),像素采样确认数字、连续天数、底部文案各带均渲染。

### D30 阶段内轻提示:准备倒数 + 休息呼吸引导(R2/R3,2026-08-05)
- **反馈**:① 开始前倒计时只有第一声,突兀;② 休息时完全没有音效,不够优雅。
- **根因(两条是同一个)**:`cueFor(phase)` 只在 `step()` 检测到阶段推进时调用一次,**阶段内部没有任何声音通道**——所以 prepare 只在进入时响一声、rest 全程静默。不是音色问题,是缺一条通道。
- **做法**:新增 `PHASE_TICKS[phase](secLeft, durationSec)` + 模块级 `lastTickSecLeft`,由已有的 100ms `step()` 循环驱动,**不新开定时器**(多一个定时器就多一份与状态机对不齐的风险)。新设置键 `softCue`(默认开),受 `sound` 总开关约束。
- **关键取舍**:
  - **判据是「阶段内剩余整秒数变了才响」,不是自增计数器**。这一条同时解决三个边界:暂停期间不响、后台切回不补拍(剩余秒数直接跳到新值 → 只响一声,而非把落后的十几拍一次性倒出来)、阶段边界自动归零。用计数器就得为每个边界单独写补偿逻辑。
  - **rest 不每秒响**:30 秒休息响 30 下与反馈里的「轻柔」正好相反。改成 4 秒吸 / 4 秒呼的呼吸节律,每半周期开头一声(吸 392Hz、呼 330Hz);最后 3 秒切 440Hz 每秒倒数,预告下一组。相位用「已过秒数」算,`restSec` 长短都对齐到进入休息那一刻;短休息(≤8s)自然退化成纯倒数,不会只响半个呼吸周期。
  - **contract/hold/relax 不加节拍**:用力阶段每秒响会盖掉 TONES 的「上行/平音/下行」方向感(D 系列一直靠这个让用户不看屏幕也知道该干什么),反而更吵。
  - **音量轻一个数量级**(peak 0.012 vs 阶段提示音 0.05):`beep` 加可选 `peak` 参数,默认值不变,老调用点零影响。节拍只当秒针用,不与提示音抢辨识度。
  - `secLeft <= 0` 那声不响:紧接着就是阶段切换的提示音,两声叠在一起听着像重音。
- **验证**:因为 app.js 不在 `npm test` 覆盖内,用一次性脚本把 `PHASE_TICKS`/`maybePhaseTick` 的真实逻辑跑成时序表:prepare 3s→3 声 587;rest 30s→392/330 交替 7 声 + 440×3;后台 28s→12s 跳变新增 ≤1 声;暂停 50 帧 0 声;`softCue=false` 0 声;用力阶段 0 声;rest 1/3/5/8s 短时长退化正确。`npm test` 78 全绿(storage 契约三处加 `softCue`),`node --check` 全过,71 个 DOM id 全部存在。

### D31 多端同步:自建甲骨文后端 + 端到端加密(2026-08-05)
- **为什么自建甲骨文(路线对比,用户拍板 C)**:见下表。核心理由:① 用户已有甲骨文 always-free 实例(沉没成本);② 用户 Cloudflare KV 多服务共用,担心「总有付费一天」,C 不碰这条计费路径;③ 一台机器解决所有未来后端需求;④ CORS 自主,告别 WebDAV/Worker 的 CORS 纠结。代价=运维在自己,但用户已在维护该机,边际成本接近零;且「觉得维护麻烦再回 Worker」是可逆决策。

  | 维度 | C 自建甲骨文(选定) | B Worker 付费 $5/月 | Worker 免费+限流 |
  |---|---|---|---|
  | 花费 | **0**(always-free 永久,已在用) | $5/月持续(年 $60) | 0 |
  | CORS | **自主**(nginx 三行头) | Worker 自带 | Worker 自带 |
  | 写入额度 | **无限**(SQLite 文件) | 100万/天 | **1000/天 ⚠️ 多用户会断** |
  | 挤占其他服务 | **不碰 Cloudflare** | 全账号进付费 | 与计数 Worker 共用免费 KV 配额 |
  | 可演进 | **一台机器吃下所有** | 每需求单评估 | 同 Worker |
  | 运维 | 在自己(nginx/进程/备份) | 最少 | 最少 |
  | 可用性 | 单机房凤凰城(同步够用,非实时) | 全球边缘 | 全球边缘 |

- **端到端加密**:PBKDF2-SHA256(200000 轮)+ AES-GCM-256。主密码绝不离开设备,后端只存 `{salt,iv,ct,iter}` 密文,永不解密、不解析 blob、不收主密码。GCM 自带完整性校验,密文被篡改→解密失败,不会解出半截乱码。iOS Safari 7+ 全支持,zero-dep。
- **安全诚实评估三段**:
  - **机密性(✅)**:AES-GCM-256 + PBKDF2-SHA256 200000 轮。主密码不离开设备,后端只见密文。**唯一限制**:主密码强度靠用户自己——弱主密码仍可被离线爆破(200000 轮/次抬高成本但不杜绝)。
  - **完整性(✅)**:由 GCM 认证标签保证。后端不解析内容,无法引入数据损坏(除了整体覆盖)。
  - **可用性/凭据泄露(⚠️ 无账号模型的权衡)**:userId 实际上是高熵的读写寻址凭据。知道它的人既能 GET 密文也能 PUT 垃圾密文覆盖;拿到密文后还可对弱主密码做离线猜解。UUID v4 的 122 位熵使远程枚举不可行,但**一旦泄露不能再声称“只会覆盖、绝对解不开”**。因此 UI 明确要求 ID 与主密码都保密、最好分开传递;本地数据仍是恢复底线。
- **花费**:现阶段 0(甲骨文 always-free 永久免费,已在用);未来 0 或迁移成本;自建无按量计费,无意外账单。诚实限制:① 单机房无 SLA,机器挂=同步断(降级本地不丢);② 国内延迟 150-250ms(同步非实时,可接受);③ 运维时间成本归用户。
- **ts schema 向后兼容**:record 可带可选 `ts`(毫秒时间戳,`app.js writeRecord` 注入 `Date.now()`,Date.now 只在胶水层);旧记录无 ts 视为 0,`mergeForSync` 内按缺失=0,旧数据不强制升级。
- **时区**:dateStr 仍是本地 `YYYY-MM-DD`(D5 防线不变);ts 只作 LWW 辅助(同 dateStr 冲突时取较大者),不含时区语义。跨时区设备在同一天内时差可能让 LWW 略有先后,但不会丢日期维度。
- **已知限制**:① PWA 不后台同步——只在打开应用时同步;② 同 dateStr 同内容(撞指纹)去重;③ **同一天多次不同内容的训练按「日」合并,LWW 只留 ts 最大的一条**(按日粒度的刻意简化;要按「次」保留需给记录加唯一 id,属设计变更);④ 主密码丢失=远端密文不可恢复(本地不丢);⑤ 限流 1 次/3s per userId + 20 次/分 per IP;⑥ 单机房凤凰城,机器挂=同步断(降级本地不丢);⑦ 迁移性:client.mjs 抽象了 origin,未来可换 Worker 后端,端到端加密不变;⑧ SQLite 自身无跨机房冗余,实际机器由 DRBS/restic → R2 备份 `/opt/sync-server/data`。
- **app.js 接线 + 主密码缓存策略 + 离线跳过 + userId 独立 key**:
  - `SYNC_ORIGIN`(自建甲骨文)与计数 Worker 的 `COUNTER_ORIGIN` **解耦**——不同址不同服务。
  - **主密码缓存策略(2026-08-05 优化)= sessionStorage 会话级缓存**(键 `tigang_sync_pass`,输入即写):tab 内刷新不丢,**关 tab 丢**;PWA 从主屏图标启动有时算新会话也丢=可接受降级(回到老流程进设置输一次,不崩)。**不进 localStorage、不进 settings/exportJSON**——硬要塞 localStorage 虽能跨会话保主密码,但凭据寿命被拉长到与用户数据同生命周期,破坏隐私基调,拒绝。诚实权衡:**sessionStorage 仍可被同源 XSS 读,与 userId(localStorage 明文)同风险等级**;要再短命只能每次输——选「会话级缓存」是便利/安全的折中。忘了主密码=远端不可恢复、本地不丢,重输=重新开始同步。
  - 离线(`!navigator.onLine`)跳过;`persist()` 后 debounce 2s 执行完整 pull→merge→push,不再盲推。
  - **不同主密码 → 解密失败 → 本地不受影响且没有 PUT**——这是端到端加密的正确表现,非 bug。
  - userId 走独立 key `tigang_sync_user`,不进 settings/exportJSON;它是高熵读写凭据而非公开编号,泄露后存在密文读取、覆盖与弱密码离线猜解风险。
- **发版注意**:app.js import 的 `sync/client.mjs` 与 `sync/coordinator.mjs` 都必须进 sw.js 预缓存清单;缺任一个都会导致更新后首次离线打开因 ES module 加载失败整站挂掉。当前缓存版本 v17。
- **仓库演进计划**:后端暂放本仓库 `sync-server/` 子目录(前后端契约同 PR、一眼对齐、首版落地最快)。`client.mjs` 强制不依赖 app.js + `origin` 参数化,为将来拆独立仓库留口子。**拆仓触发条件**:time-logger 要同步时,把 `sync-server/` 整体迁出成独立仓库(拟名 `eigentime-sync`),tigang-companion 与 time-logger 通过 git submodule 或 npm link 引用;客户端代码零改动,只是后端服务换仓库地址。遵循 ROADMAP「先验证再投入」原则——单仓库够用时不提前拆。

### D32 计数徽标化:空闲态紧凑徽标 + 训练中隐藏(R5,2026-08-05)
- **反馈**:顶栏第二行整宽常驻「此刻 X 人在做 · 总访问 Y」太侵入,移动端窄屏占位、跟连续天数芯片抢注意力(R5)。D26 当初放 header 的理由是「陪伴感靠常驻可见」。
- **调研**(HANDOVER §3)列了 5 个候选,按「侵入性 vs 可见性」权衡。用户拍板方案 a:**空闲态显示 + 紧凑徽标**,并保留两数(「● X 人在练 · 总访问 Y」)。
- **实现**:
  - `index.html`:`.site-stats` 文案改 `● X 人在练 · 总访问 Y`,加初始 `hidden`。
  - `styles.css`:整宽第二行(`flex:0 0 100%`)→ 紧凑 pill(`flex:0 0 auto` + `padding:3px 10px` + `border-radius:999px` + `nowrap`),仍居中于 header。
  - `app.js`:`renderTrain()` 里 `el.siteStats.hidden = isRunning(session)`——每 100ms 渲染帧同步,训练中隐藏、空闲/完成态显示;完成/重置重渲染回空闲态徽标自动回来。
- **口径**:总访问**保持 PV 不改**(刷新+1 是 D26 定的 PV 口径,用户明确确认不改)。计数协议不动(worker 不碰,仍由 counter 模块 + renderCounter 更新),只改 DOM/样式与显隐时机。
- **为什么用 renderTrain 驱动而非显式事件**:显隐要覆盖 开始/暂停/继续/完成/重置/后台切回 所有路径,而 renderTrain 每 100ms 无条件跑,`hidden = running` 是纯状态投影,天然覆盖所有路径,零遗漏。代价是每帧多一次赋值,无感。

### D33 同步安全审计 + 修复(审计发现 1-3,2026-08-07)

> 历史记录:D33 的布尔门闸方案已被 D34 的 generation 编排器整体替代;当前实现与准据看 D34/SPEC §6.z。

用户提了关键问题:「如果两个用户都用 1234 当密码,会不会相互覆盖」——这触发了完整审计,找到三条需要修的(core/sync 加 pure fn 其余 app/server/ui/贴中)。

#### 审计发现 1(🔴 严重):解密失败后无条件推送会覆盖云端正确密文

`syncNow()` 的流程是 `doSyncPull()` → `doSyncPush()`, **串行且无条件**。解密失败时 `doSyncPull` 只是 `syncNowState('解密失败…')` 然后 `return` → **紧接着 `doSyncPush()` 照樣执行**,把本机数据用错误主密码加密后推上去覆盖正确的远端密文。后果:设备 B 填对了 userId 但主密码记错一个字 → 设备 A 的云端数据被 B 的密文覆盖 → A 下次同步也"解密失败"→ 两台设备的远端同步状态互相摧毁,各自本地数据不丢但云端毁了。

修复:
- 模块级 `syncDecryptFailed` 标志, `doSyncPull` 解密失败→置 true
- `doSyncPush` 开头检查:若 true→拒绝推送、显示"主密码不符,未上传(避免覆盖云端数据)", **syncNow 也检查该标志,非首次 push 跳过**
- `scheduleSyncPush` 检查该标志(离线免推不覆盖)
- 主密码 input 事件→置 false(道歉重试)
- 再次 pull 解密成功→置 false

#### 审计发现 2(核心):userId 寻址缺陷——两台设备永远同步不到一起

`newUserId()` 是随机 UUID v4,**每台设备各生成不同的 ID→存在不同的后端桶→永远同步不到一起。这才是用户实际报告的 bug。

方案评估:
- **从主密码派生(id=PBKDF2 密码+固定 salt)**——相同主密码→相同 userId 自动指对桶。**拒绝:撞桶风险**;两个用户都用 `1234`→同一个桶→互相覆盖密文→双方都"解密失败"交替.派生不因固定 salt 降低对弱主密码的攻击面(拿到 userId 可离线爆破)且弱密码在用户群中自然撞车
- **设备 A 显示 userId,设备 B 手填**(用户决定,推荐)——不可猜(UUID v4 122 位),防范撞桶(不同用户不同 ID),多端确指对桶;**代价 = 一 copy-paste**。

从用户直接指明:**不要派生**,手填 + 复制按钮。理由: `1234` 这种弱密码在用户群中自撞率是很高的,派生方案会把"两个用户撞密"升格为"云端数据互相摧毁",对手填方案不存在。因此审计 2 的修复核心:点对点同步 = 人的操作。所有设计已基于此。

#### 审计发现 3(健壮性):后端接受任意字符串作 key,测试数据成潜在陷阱桶

`server.mjs` 仅检 `MAX_KEY_LEN=128`,**不验 UUID 格式**。测试时已用 curl 写入 `key=testuser`→ 若手填时误入此串→解密失败→又篡改覆盖。

修复:① 客户端 `normalizeUserId()` 纯函数 UUID v4 校验+(前后端双层);② 后端 `UUID_RE` 含 `not` 都双向;③ 部署前手动清测试桶

修复(3 条合并实现):
- `core/sync.js` 新增 `normalizeUserId(raw)`(纯函数,可测,含去空白/统一小写/UUID v4 正则) → `tests/sync.test.mjs` 已覆盖 8 个合法/非法/边界用例
- `sync-server/server.mjs`:限流间隔 `USER_PUT_INTERVAL_MS` 10s→3s;`MAX_KEY_LEN` 之前已有;新增 `UUID_RE`(UUID v4 正则)→仅 PUT/GET key 必须匹配→ `bad-key` 拒绝

#### 审计通过/记录的取舍(留档,非修复)

- **userId 清空后旧桶成孤儿**:后端永远不再被访问。轻微存储泄漏,个人站量级毛不处理,记已知限制
- **userId 猜立不可行**:UUID v4 122 位 + per-IP 20 次/分限流,枚举不可行
- **userId 泄露的后果可控**:攻击者最多覆盖密文(DoS),拿不到明文(主密码).且在修复发现 1 后,受害者本地数据完好,重推即恢复
- **并发推送**:两设备同时 push→后端 last-write-wins,但客户端 pull→merge→push 且 mergeForSync 是加性合并(双方记录都保留),最坏某次 push 基于略旧的数据,下次同步补齐。已知"同日同内容 LWW 只留一条"限制不变(D31 限制③)

#### 撞桶论证(为什么手填而不用派生,完)

- 派生方案:同主密码 → 同桶 → 两堆密文集交疊 → 互相覆盖 → 双方"解密失败"→ 云端数据真被毁
- 手填方案:每设备独立 uuid → 桶天然隔离 → 自愿链接后才共享桶 → 用户自行接受"覆盖风险"且知道后果。而且弱密码在用户群中自然撞车比故意攻击概率高得多
- 结论:手填更安全(不引入同密码撞桶的 DoS 通道),审计通过

#### 布局修正(D32 后继,同 commit)

- `.dlg`:`max-height: min(88vh, 650px)` + `overflow-y:auto`;`.dlg-actions` sticky bottom
- `.coach-circle`:默认 160px(mobile) / ≥420px 200px(修正原断点方向,小屏反拿大圆)
- `.card`:margin 14→10px,padding 16→14px(窄屏省控)

### D34 同步编排二次验收:从布尔门闸升级为身份绑定流水线(2026-08-07)

D33 修掉了「解密失败后继续 push」这一条路径,但二次验收发现问题本质比一个布尔门闸更大:

- GET 网络/HTTP 失败时 `doSyncPull()` 仍只 return,`syncNow()` 随后照样 PUT,会用未合并的本地快照覆盖远端。
- 应用新 userId 时没有取消既有 debounce、429 retry 或在途请求;旧任务在 `await` 后读取新的全局 `syncUserId`,存在串桶风险。
- `persist()` 自动同步直接 encrypt→push,没有先拉取远端;多设备同时活跃时会扩大 last-write-wins 的丢更新窗口。
- 429 重试重放旧 PUT,等待期间远端若有新变化仍可能被旧快照覆盖。
- 首次启用只在真正网络请求时才生成 ID,且生成后不刷新输入框,主流程必须关掉再打开设置才能复制。

修复不是继续叠标志,而是新增独立 `sync/coordinator.mjs`,建立三条硬不变量:

1. **PUT 前置条件**:同一 generation + 同一 userId/passphrase 下,本轮 GET 必须成功解密并合并,或明确返回 `none`;任何拉取/解密失败都结束本轮。
2. **身份生命周期**:userId、主密码、启用状态任一变化 → generation++、清 debounce/retry timer、AbortController 取消 fetch;每个异步步骤只使用启动时快照,旧结果不得落地或更新 UI。
3. **所有入口同路**:手动、启动、`persist()` 防抖、429 重试全部走 pull→merge→深快照→encrypt→push;重试必须重新 GET。同步期间又有本地变化则记 rerun,当前轮结束后再跑一轮。

配套:

- `sync/client.mjs` 支持外部 AbortSignal,GET 强制 `cache:'no-store'`;服务端也回 `Cache-Control:no-store`。
- 开启同步立即生成/显示 ID;同步中禁用「立即同步/应用 ID」只是 UX,安全仍由 generation 保证。
- `tests/sync-coordinator.test.mjs` 用 deferred promise + fake timer 覆盖网络失败不推、pull/encrypt 中换 ID、timer 取消、429 重新拉取、rerun 与深快照。
- systemd 明确绑定 docker0 `172.17.0.1`,修复「文档让容器连 docker0,进程却只监听 loopback」的不可复现部署。
- 圆尺寸媒体查询按既定契约修正:≥420px 才 200px,极矮屏强制 160px。

### D35 「有了 UUID 还要主密码吗」+ 安全加固四项(2026-08-08)

用户问:既然 userId 已经是 122 位熵的 UUID v4,主密码是不是多余的?

**结论:不能删,而且它是这套设计里唯一不能删的东西。** 两者解决的不是同一个问题:

| | userId (UUID v4) | 主密码 |
|---|---|---|
| 作用 | 寻址 + 读写凭据(bearer token) | 机密性(AES-GCM 密钥来源) |
| 在哪出现 | URL query、localStorage 明文、(修复前)nginx 日志 | 只在内存 + sessionStorage,**永不上网** |
| 泄露后果 | 能读密文、能覆盖密文 | 配合密文可解出明文 |

关键在于**userId 是会流动的,主密码不流动**。UI 明确鼓励用户把同步 ID 从设备 A 复制到设备 B(微信、备忘录、剪贴板),它还出现在 `?key=` 里。一个会被到处复制的东西不能同时当解密钥匙。`GET /sync?key=` 是无鉴权的——任何人拿到 UUID 就能读。D33 写下的「泄露最多被覆盖、拿不到明文」这句话**完全建立在主密码之上**,删掉它这句就不成立了,后端也从「零信任」退回「你得信任我不看、不被入侵、备份不泄露」。

**够安全吗:密码学原语没问题,工程上有四个真实缺口,本次全修。**

1. 🔴 **主密码无任何长度下限**(最严重)。`#opt-sync-master` 没有 minlength,app.js 也不校验;用户录屏里输的就是 3-4 位。userId 一旦泄露,4 位密码离线爆破是秒级——搜索空间才 10^4,PBKDF2 轮数只抬高单次成本、**不缩小搜索空间**,任何轮数都救不回来。这一条把整个加密体系的强度拉回到接近于零。→ `core/sync.js` 新增纯函数 `checkPassphrase`(< 8 位拒绝;纯数字 < 12 位拒绝),UI 实时提示 + 不达标时**不给 coordinator 设密码、不发任何请求**。下限必须由程序守住,写在小字 hint 里等于没写。
2. 🔴 **userId 进日志**。nginx 默认 log_format 记完整 URI,`?key=<uuid>` 直接落盘;后端 429/400 两个分支也 `console.log(req.url)`,journal 里同样有。凭据进日志 = 日志泄露即凭据泄露,而日志通常比数据库更容易被顺手打包带走。→ nginx 该 location `access_log off`(error_log 保留,它不记 query),后端统一走 `logLine()`:剥掉 query 只留 pathname,key 只打前 8 位用于关联排查。
3. **`iter` 来自后端且未校验**。`decryptBlob` 直接信 `blob.iter`;恶意/被入侵的后端返回 `iter:1e9` 就能把客户端冻死在 `deriveKey` 上(主线程 DoS),返回 `iter:1` 则试图把爆破成本降到可在线穷举。→ 加 `MIN/MAX_ACCEPTED_ITERATIONS`(10 万–100 万)区间校验。**选择拒绝(`bad-iter`)而不是静默夹取**:静默改数会让「后端在撒谎」看起来像「用户密码错」,这两件事值得区分。
4. **轮数 200000 偏低**。OWASP 对 PBKDF2-HMAC-SHA256 的现行建议是 600000。→ 提到 600000。**升级天然向后兼容**:轮数随 blob 存(`iter` 字段),旧密文按自己的 iter 解,只有新写入的才用新值;成本只是首次同步多两三百毫秒。测试里手工造了一个 200000 轮的 blob 锁住这条兼容性。

**另加:解密失败的出路。** 原本没有——D33 的门闸锁死一切 push 是**对的**(防止用错密码覆盖云端),但对本人来说这意味着记错/改过主密码后永久卡在「主密码不符」,没有任何前进的方式。补 `#sync-recover`(仅解密失败时出现)两个显式出口:`overwriteRemote()`(本机整桶覆盖远端)与「换一个新同步 ID」。`overwriteRemote` 是**唯一**可以清 `decryptFailed` 的入口,且仍然先 GET——不变量「PUT 前必须有同身份成功 GET」照旧成立,只是明知故犯地跳过合并。429 重试必须把 `overwrite` 标志一起带上,否则重试走回普通流程又在解密处失败,用户点了「覆盖」却什么也没发生。

**仍然接受的限制**:主密码强度终究靠用户(下限只挡住最坏的一档);sessionStorage 可被同源 XSS 读,与 userId 同风险等级;单机房无冗余。这些在 D31/D33 已记录,本次未改变。

### D36 设置精简 + 训练页优雅化(2026-08-08,同 commit)

依据用户 29 秒录屏逐帧核对,不是凭想象改。

**设置弹窗**:七个开关平铺 + 同步区块常驻展开,录屏里滚动条只占轨道 1/3,要滚两屏才到底。四刀:

- **同步折叠成单行入口**(收益最大)。它占了弹窗约六成高度,而绝大多数用户从不开它。改成 `#btn-sync-entry` 一行摘要(右侧显示未开启/已开启),点开二级 `#dlg-sync`。二级弹窗独立而非行内展开,是因为它自己就有六个控件 + 状态行 + 恢复出口,塞回一级等于没精简。
- **提醒时间行随开关走**。原来无条件显示,关着提醒也白占一行——跟 `hold-sec-wrap` 一样 hidden 掉。
- **轻提示降为提示音子项**。app.js 早就有 `optSoftCue.disabled = !sound`,逻辑上它本来就是附属,视觉上却是同级。缩进 + 左竖线 + 小一号字,让层级与约束一致。
- **说明文字下沉**。原本三段 hint 全堆在弹窗底部,各自对应上面很远的某个开关,读者要自己连线。拆回各自开关下方,并加「声音 / 提醒 / 同步」分组小标题。

结果:首屏能装下全部主开关。

**训练页**(问题集中在空闲态):

- **条形进度条 → 圆周进度环**。这是让页面显著变优雅的单点改动:圆原本只是个会变色的实心盘(纯显示器),外面套一圈 `conic-gradient` 环之后变成仪表,同时干掉了下方那条空闲时什么都不表达的灰轨。**踩坑**:环不能做成圆的父元素——环靠 `mask: radial-gradient(...)` 把中心掏空才成为「环」,而 mask 会连同子元素一起裁,圆放进去就被掏没了;必须三层平铺,`.coach-ring` 与 `.coach-stage` 用 `grid-area:1/1` 叠在同一格里(不用 `position:absolute`:绝对定位子元素在 grid 容器里的静态位置各家实现不一致)。另外环必须固定尺寸——圆会 `scale(0.62)` 呼吸,环若跟着缩放,进度就读不准了。
- **圆改 `<button>` 可点**(空闲=开始,进行中=暂停)。语音/AudioContext 首次调用仍在真实点击处理器内,iOS 那条约束不破。
- **空闲态 `—` → `▶`**。破折号读起来像「没数据/坏了」,不像「待命」。
- **按钮按状态出**。空闲态三按钮里两个是 disabled 灰块,白占 2/3 面积;改成空闲整宽「开始训练」,进行中才出暂停 + 结束。
- **`-webkit-tap-highlight-color: transparent`**。录屏第 10 秒设置齿轮上有个安卓默认蓝色方块,跟整套青色系很违和。
- **空闲态留白**。录屏里方案卡与圆之间空了近 300px,上下大片死白。**没有改布局结构**——`.coach` 的 `flex:1` 是有意的:它把控制按钮顶到屏幕下方,拇指够得着。改的是视觉重量:圆外多了一圈 216px 的进度环(空闲态留白但仍占位),中心区不再是孤零零一个 160px 的盘,死白感随之减轻。若后续仍嫌空,再考虑给 `.coach` 加 `max-height` 让余量单独落到底部。

### D8 已知限制 / Backlog
- **提醒**:无后端 ⇒ 无 Web Push;通知仅在页面打开时由 setTimeout 触发。iOS 需 16.4+ 且安装到主屏才有通知能力。若要可靠提醒,后续加个极简 push 服务或打包原生。
- ~~图标仅 SVG~~:已解决(2026-08-02,见 D15)——补齐 180/192/512/512-maskable 四个 PNG,解决 iOS 主屏图标退化问题。
- 未做:深色模式、数据导入(只有导出)、多用户、云同步(有意不做,隐私优先)。
