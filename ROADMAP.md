# ROADMAP — 让「提肛陪伴」被更多人用起来

> 一个健康陪伴工具要"爆",拼的不是功能堆砌,而是**被看见 → 愿意试 → 愿意回来 → 愿意分享**的闭环。
> 原则(参考 time-logger 的「不镀金」):每一项都先问「它服务的是哪一环、怎么验证效果」,没有可验证目标的机制不写进近期。
> 实现约束不变:零依赖、无后端、数据本地、隐私优先。唯二的例外(均 opt-in,不碰第三方):① 顶栏全站计数 = 自建 Cloudflare Worker(worker/);② 多端同步 = 自建甲骨文后端 sync-server/(端到端加密,后端只见密文,主密码不离开设备)。
> 英文名(拟):**KegelMate**(品牌页已用;要换只改 site/ 与 manifest 各一行)。

---

## 增长飞轮(目标结构)

```
搜索/分享进来 ──► 落地页讲清价值 ──► 打开应用免费开练
     ▲                                     │
     │                                     ▼
  分享卡片/文案 ◄── 完成一次训练(成就时刻) ◄─ 首练顺利
     │
     └── 朋友看到 → 回来;连续打卡 → 每天回来
```

## Next — 近期可做(零 / 极低新增依赖)

| # | 机制 | 服务飞轮哪一环 | 做法 | 验证方式 |
|---|---|---|---|---|
| N1 | **训练完成分享卡** ✅ 已完成 | 分享裂变 | 完成面板加「分享」:canvas 画一张「今日收缩 X 次 · 连续 Y 天」卡片 → Web Share API / 长按保存。零新依赖 | 分享按钮点击率 |
| N2 | **一键文案** | 分享裂变 | 「今天也完成了 X 次凯格尔,连续第 Y 天。免费、数据只在本地,要不要一起?」+ 链接,一键复制 | 复制次数 |
| N3 | **「你是第 N 位使用者」** | 被看见 / 荣誉感 | 复用已建 DO:给 visitorId 发全局序号(原子递增),文案诚实写「约第 N 位开启训练的人」;需防重装重复计数,先把口径定死 | 展示正确性 |
| N4 | **安装引导** | 留存 | 检测未安装 + 使用 ≥ 3 次后,首屏出现「添加到主屏」引导(iOS/Android 分别给步骤);装进主屏是 PWA 留存的关键拐点 | 安装转化率 |
| N5 | **断签挽回** | 留存 | 断签后首屏提示「昨天断了,今天开始还来得及」+ 一键开始;降低放弃率 | 断签次日回访率 |
| N6 | **SEO 落地页细化** | 被看见 | 落地页(已完成初版)补「怎么练」「为什么练」可读长文、结构化数据;让「凯格尔训练」「提肛训练」能被搜到 | 搜索落地页访问量 |
| N7 | **多端同步** ✅ 已完成(撞旧 G2,已解 gate) | 留存(换设备不丢) | **opt-in + 端到端加密**(PBKDF2 200000 + AES-GCM,主密码不离开设备)+ **自建后端只见密文**(`sync-server/`,甲骨文 always-free);启动/数据变化/限流重试统一走 pull→merge→push,离线纯本地 | 用户启用率 / 换设备无感;不同主密码 → 无 PUT、降级本地 |

## 反馈 backlog(2026-08-05,详细见 HANDOVER.md)

| # | 反馈 | 归属 |
|---|---|---|
| R1 | 导入/导出更扎实,不同设备间记录处理好(不做云同步,撞 G2) | 近期可做,零新依赖 |
| R2 | 开始前倒计时只有第一声,建议每秒轻 tick | ✅ 已完成(D30) |
| R3 | 休息时无持续音效,建议轻柔节拍/呼吸引导(可做成开关) | ✅ 已完成(D30,4 秒吸/4 秒呼 + 末段倒数,设置里「轻提示」可关) |
| R4 | 排行榜 or 讨论区? | 撞 G1,维持 gated,不做 |
| R5 | 计数固定 header 太侵入 → ✅ 已完成(HANDOVER §3 方案 a:空闲态紧凑徽标,训练中隐藏) | 改版完成 |

## Mid — 中期(需扩展 Worker 或较大改动)

| # | 机制 | 说明 |
|---|---|---|
| M1 | **Web Push 每日提醒** | 现在提醒仅在页面打开时生效。要真正触达需 Web Push:现有计数 Worker 扩展为 push 服务(订阅 + 定时推送),iOS 16.4+ 支持。触达率是陪伴类产品留存上限 |
| M2 | **全球「今日已练 X 次」实时合计** | 复用 DO:匿名合计今日收缩次数,顶栏/统计页展示「今天全网一起练了 X 次」——把独自练习变成共同参与 |

## Gated — 需先批准或明确不做

| # | 机制 | 为什么 gated |
|---|---|---|
| G1 | **排行榜 / 组队打卡** | 需要匿名参与者 + 后端 + 明确的隐私口径;参考 time-logger R3 的诚实论证——要么做出可信数字,要么不做,不能糊一个假数 |
| ~~G2~~ | ~~云同步 / 多设备~~ | **已解 gate**(N7 已完成):opt-in + 端到端加密 + 自建后端只见密文 |
| G3 | **原生 App / iOS 主屏挂件** | 字面的锁屏/灵动岛挂件需原生;会撞零依赖身份,等于换一个项目,默认不做 |
| G4 | **深色模式 / 主题换肤** | 纯打磨,不服务增长;等到有「分享卡片需要多风格」的真实需求再做 |

## 原则重申

- **先验证再投入**:N 系列每项都有「验证方式」列,效果没到阈值就不升级成 M 系列。
- **隐私是卖点不是负担**:「默认本地、同步只上传端到端密文、不引第三方统计」本身就是差异化,别为增长偷偷让步。
- **口径诚实**:任何「第 N 位」「全网 X 次」都必须是真数字或明确标注估算,不制造虚假社会证明。
