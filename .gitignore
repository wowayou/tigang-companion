.DS_Store
*.log
node_modules/

# 参考截图等本地素材,不进仓库
外部/

# 部署组装产物(tools/build-site.mjs 生成,CI 会重建)
dist/

# wrangler 本地开发状态(worker/.wrangler 由 wrangler dev 生成)
worker/.wrangler/

# 同步后端运行时数据(SQLite 文件,由 server.mjs 运行时生成,不进仓库)
sync-server/data/
